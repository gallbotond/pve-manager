Sample `proxmox.env' file (has to be in the same directory as the script):

```bash
# proxmox.env

API_URL="https://your-proxmox-host:8006/api2/json"
TOKEN_ID="root@pam!apitoken"
TOKEN_SECRET="your-api-token-secret"
```

